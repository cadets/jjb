- project:
    name: cadets-bsd
    branch:
      - cadets
      - cadets-e3
      - llprov
    jobs:
      - 'bsd-ci-{branch}'

- project:
    name: bsd-pull_request
    jobs:
      - 'bsd-ci-pull_request'
